import 'package:flutter/material.dart';

class StoreRatingPage extends StatelessWidget {
  const StoreRatingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('store rating page'),
    );
  }
}
